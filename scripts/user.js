export const Results = (resultsobject) => {
   return `
     <section class="results">
       <header>
           <h2 class="result__title">${resultsobject.cell}</h2>
       </header>
     </section>
   `
 }

 
